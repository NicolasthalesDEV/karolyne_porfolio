import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ['seudominio.com'], // coloque aqui se houver imagens externas
  },
};


export default nextConfig;
